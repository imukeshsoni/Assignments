Q1.

const anExampleVariable = "Hello World";
anExampleVariable = "Hi World"; //Throws an Error that const variable can not be reassigned
console.log(anExampleVariable);

Q2.
let a = 10;
    if(a===10){
        let b = 20;
        console.log(b);
    }
    console.log(b); //Throws an error "b is not defined"

Q3.
let order = {
    id : 10,
    title: "test",
    price: 100,
    printOrder: () => "Order printed",
    getPrice: function (){
        return this.price;
    }
}

console.log( order.getPrice());

let order1={};

Object.assign(order1, order);

console.log( "order 1 price = "+ order1.getPrice());

Q4. 


let names = [`Tom`, `jerry`, `Ivan`];

let newArray = names.map(countLength);

function countLength(n){
    let length = n.length;
    return {name: n, length: length}
}

console.log(newArray);